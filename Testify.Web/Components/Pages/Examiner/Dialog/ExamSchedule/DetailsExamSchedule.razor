<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">

            Chi tiết lịch thi
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField T="string" Disabled Label="Mã lịch thi" ReadOnly="true" />
        <MudTextField T="string" Disabled Label="Mã bài thi" ReadOnly="true" />
        <MudTextField T="string" Disabled Label="Thời gian bắt đầu" ReadOnly="true" />
        <MudTextField T="string" Disabled Label="Thời gian kết thúc" ReadOnly="true" />
        <MudTextField T="string" Disabled Label="Trạng thái" ReadOnly="true" />

    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Surface" Variant="@Variant.Filled" OnClick=" Cancel">Quay lại</MudButton>
        @* <MudButton Color="Color.Primary" OnClick="Submit">Tạo</MudButton> *@
    </DialogActions>
</MudDialog>
@code {
    [CascadingParameter]
    private MudDialogInstance MudDialog { get; set; }

    private void Submit() => MudDialog.Close(DialogResult.Ok(true));

    private void Cancel() => MudDialog.Cancel();
}
